The first goal of this task is to correlate the two color codifications, "simple" and "RHS", used in Agrosavia.
The second goal calculate correlations between color base phenotypes

The first script creates the tables for calculating the correlations, and the second calculates and plots the correlations.

Column names are taken from a table in the "inputs" dir
